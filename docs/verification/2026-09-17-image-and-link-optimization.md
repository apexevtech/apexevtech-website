# 图片与链接检查 — 2026-09-17

用户要求继续其他工作。先执行网站速度/使用体验和链接检查。

## 修改

- 产品卡片增加响应式 sizes，首页与产品目录分别匹配各自列布局。
- 产品详情主图使用固定4:3容器及 fill/object-contain，移除至少460px高和桌面随整行拉伸的空白区域；设备图片完整显示。
- 主图增加 priority，避免重要产品图被延迟加载；其余产品卡片继续默认延迟加载。

## 验证

- 正式网站遍历28个 sitemap 页面、42个内部链接目标，包括产品资料下载和锚点，没有失效链接或缺失锚点。Unicode文档路径按URL编码后验证。
- 本地生产构建含类型检查通过，图片资源及内部导航9项测试通过。
- 浏览器390px与1440px视口检查首页、产品列表、ST-9980A+ Pro详情，无横向溢出。
- 原正式产品图在390px视口显示宽284px，却选择1080px图，延迟加载，父容器高460px。
- 本地修改后同样390px视口选择384px图，主图不延迟加载，容器高262.5px；1440px视口选择640px图，容器高约438.9px。
- 同一产品/同一q=75下实际HTTP下载：384px图7814字节，1080px图38231字节，约减少80%。此比较只代表该图片及此次请求，不能声称全站速度提高80%。高DPR设备可能选择更大候选图。

此次未进行真实用户 Core Web Vitals 测量，不承诺LCP分数提升。生产数据需后续观察。

正式发布完成：dpl_B3dur64xAX6oyddeUiwMyWzYCqPe，READY，正式域名 www.link-jl.com。线上390px视口主图选择384px候选图，容器262.5px高，不延迟加载；无横向溢出。
